apiVersion: ar.dev/v1alpha1
kind: MCPServer
metadata:
  name: josh-pritchard/everything-server
  version: "0.1.0"
spec:
  title: everything-server
  description: Demo MCP server exposing echo, now, add, and reverse tools.
  packages:
    - registryType: oci
      # Set this to a registry path you can push to, e.g.
      #   ghcr.io/<your-github-user>/everything-server:0.1.0
      identifier: us-docker.pkg.dev/developers-369321/agentregistry-enterprise-public-nonprod/josh-pritchard/everything-server:0.1.0
      version: "0.1.0"
      transport:
        type: stdio
